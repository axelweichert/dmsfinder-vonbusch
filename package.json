{
  "name": "vonbusch-crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "wrangler deploy",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply vonbusch-crm --remote",
    "db:seed": "wrangler d1 execute vonbusch-crm --remote --file=migrations/seed.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.28.4"
  },
  "dependencies": {
    "hono": "^4.1.0"
  }
}